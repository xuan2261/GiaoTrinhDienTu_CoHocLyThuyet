# Release Gate Report — 2026-05-18

Plan: `plans/260518-2100-fix-formula-image-and-dupes/`
Title: Formula-as-Image, Duplicate Render & Alt-Text Hardening
Mode: `/cook --tdd` (interactive TDD across 7 phases)

## Phase outcomes

| Phase | Title | Status | TDD test |
|---|---|---|---|
| 01 | Baseline & TDD test infrastructure | PASS | `test-phase-01-baseline-html-chapter-formula-image-ref-counts.py` |
| 02 | Fix 8 critical formula-as-image | PASS | `test-phase-02-eight-critical-formula-image-replacements-deletions.py` |
| 03 | Deduplicate 40 MathML+KaTeX render pairs | PASS | `test-phase-03-no-duplicate-mathml-katex-render-pairs-in-html.py` + `test-phase-03-pipeline-extract-docx-no-duplicate-regeneration.py` (Front B) |
| 04 | Standardize render font + KaTeX htmlAndMathml | PASS | `test-phase-04-equations-css-render-font-and-katex-htmlandmathml.py` |
| 05 | Alt text + figcaption + `<figure>` migration | PASS | `test-phase-05-alt-text-figcaption-figure-tag-migration.py` |
| 06 | Audit guard `--strict-formula-image` default ON | PASS | `test-phase-06-audit-strict-formula-image-guard-default-on.py` |
| 07 | Final QA + docs sync | PASS | `test-phase-07-final-release-gate-playwright-smoke-docs-sync.py` |

## Quantitative outcomes

| Metric | Before | After |
|---|---|---|
| Chapter HTML files | 120 | 120 |
| Total `<img>` in chapters | 136 | 127 |
| Critical formula-as-image (Phase 02 OCR scope) | 8 | 0 |
| Duplicate MathML+KaTeX render pairs | 40 | 0 |
| Generic `Hình minh hoạ chương X` alts | 134 | 0 |
| `<figure>` with `<figcaption>` | 0 | 127 / 127 |
| `<div class="figure-container">` legacy wrappers | 127 | 0 |
| `audit.py --strict-formula-image` flagged suspects | n/a (flag did not exist) | 0 |
| KaTeX `output` mode | `html` | `htmlAndMathml` |
| Allowlist exceptions | n/a | 1 (`images/ch1/hinh-172.png` — line drawing problem-statement) |

## Verification commands

```powershell
npm run test:sim:unit                               # PASS — 104 JS files + 7 unit suites
npm run test:equations                              # PASS — 7 phase TDD scripts
python tools/audit.py --strict-images --strict-formula-image
                                                    # PASS — 0 errors, 0 strict suspects
python scripts/audit-all-formula-as-image.py        # 0 references to the 8 deleted Phase 02 paths
python scripts/detect-duplicate-math-broad.py       # Grand total duplicates: 0
python tools/bundle_pages.py                        # 1224.7 KB pages.js, no deleted-image refs
```

## Gates not run in this session

- `npm run test:sim:browser` (173 Playwright tests, requires `playwright install chromium`)
- `npm run test:sim:visual-quality` (4 Playwright visual tests)
- `npm run test:sim:disposal`
- `npm run test:sim:release` (full release gate, runs all of the above + `--strict-formula-image`)

These all require Playwright + a running Chromium binary; deferred to the user's CI run after the commit. The `test:sim:release` script in `package.json` already chains `--strict-formula-image` (F2.5).

## Gates run in this session (Playwright)

- `npx playwright test tests/visual/phase-07-smoke-visual-regression-deleted-images-math-rendering.spec.js` — 9 passed (4.3s)

## Files added

- `scripts/equations-fix-shared-test-helpers-html-image-utilities.py`
- `scripts/test-phase-{01..07}-*.py` (7 plain-Python TDD scripts)
- `scripts/measure-docx-caption-coverage-for-figure-alt-text-generation.py`
- `scripts/replace-eight-critical-formula-as-image-with-inline-katex-or-delete.py`
- `scripts/dedupe-mathml-and-katex-render-pairs-keep-mathml.py`
- `scripts/parse-docx-figure-captions-emit-suggested-alt-and-figcaption-csv.py`
- `scripts/build-image-alt-overrides-json-from-suggested-alt-csv.py`
- `scripts/apply-image-alt-text-and-figcaption-from-overrides-and-docx-captions.py`
- `scripts/fill-remaining-figure-alt-and-figcaption-from-section-title-fallback.py`
- `scripts/git-tag-with-retry.ps1`
- `tests/visual/phase-07-smoke-visual-regression-deleted-images-math-rendering.spec.js`
- `css/equations-and-figure-styling-mathml-katex-font-sync-figure-figcaption.css`
- `data/image_alt_overrides.json` (88 SHA1[:12] → alt+figcaption)
- `data/formula-image-allowlist.json` (1 entry: `images/ch1/hinh-172.png`)

## Files modified

- `tools/audit.py` — added `--strict-formula-image` (BooleanOptionalAction default True), `--root` arg, `strict_formula_image_errors()`, PNG metadata reader, before-text capture, `<figure>` regex support
- `tools/extract_docx.py` — wired `_run_auto_fix_known_issues()` post-processor with `--auto-fix-known-issues` (default True)
- `js/loader.js` — KaTeX `output: 'htmlAndMathml'` for screen-reader access
- `index.html` — link to new equations CSS
- `package.json` — `test:equations`, `test:audit:strict`, and `test:sim:release` strengthened
- 50 chapter HTML files — figure migration, alt/figcaption updates, dedupe, formula migration
- `js/pages.js` — regenerated by `bundle_pages.py`
- `docs/project-changelog.md`, `docs/code-standards.md`, `docs/project-roadmap.md`

## Files deleted

- `images/ch3/hinh-266.png` (Phase 02 OCR delete)
- `images/ch3/hinh-283.png` (Phase 02 OCR delete)

## Allowlist rationale

`images/ch1/hinh-172.png` (704×672, 3.0 KB, 1-bit) tripped the `1-bit + small-file + keyword` heuristic because it sits next to the keyword `phản lực` (reaction force) in `ch1/muc-VII-1.html` Bài 1. Manual inspection: it is the actual line-drawing of a clamped beam AC for the problem statement, not a formula glyph. Added to `data/formula-image-allowlist.json`.

## Boundary respected

Per plan boundary reminder, the simulation engine (`js/sim-*.js`, `js/sims/*`) and `tests/promax-pilot-shell.spec.js` were NOT modified. `data/equation_mapping.json` was not edited; the 8 critical entries are not present in its `examples[].output` keys (the 8 raster images are figure-class, not equation-mapping outputs), so the F2.4 obsolete-mark step in the plan was N/A.

## Next steps for the user

1. Manually review the 50 changed chapter files and the 88 alt-overrides for Vietnamese accuracy. Section-title-fallback alts (42 of them) use the closest `l3-title` or `h2`, which gives accurate but generic results — consider hand-tuning specific ones if needed.
2. Run the full Playwright suite (`npm run test:sim:browser` + `npm run test:sim:visual-quality` + new `phase-07-smoke-*.spec.js`) on a machine with Chromium available.
3. Run `npm run test:sim:release` for the full release gate with `--strict-formula-image`.
4. If all green, commit with conventional message and tag via `scripts/git-tag-with-retry.ps1 -BaseTag "v-2026-05-18-formula-fix" -Message "..."`.

## Unresolved questions

- 2 pre-existing critical formula-as-image entries (`images/ch1/hinh-078.png`, `images/ch2/hinh-211.png`) reported by `scripts/audit-all-formula-as-image.py` are OUT of the Phase 02 OCR scope (only 8 entries verified). They are equation-mapping rows whose `mapping_no_render` flags pre-date this plan and should be addressed in a follow-up sweep, not this release.
- Phase 03 Front B (`tools/extract_docx.py` root-cause regeneration test) is now wired: `scripts/test-phase-03-pipeline-extract-docx-no-duplicate-regeneration.py` asserts `--auto-fix-known-issues` flag, `def _run_auto_fix_known_issues` helper, dedupe script in candidate list, `--idempotent` invocation; runs `dedupe ... --check` and verifies `Files to change: 0 | total pairs: 0`. Re-extract idempotency is covered by the post-processor (`--auto-fix-known-issues`, default ON).
- The Playwright spec was wet-tested in this session: `npx playwright test tests/visual/phase-07-smoke-visual-regression-deleted-images-math-rendering.spec.js` → 9 passed (4.3s).
